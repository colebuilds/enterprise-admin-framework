# i18n 1.5：Locale 文件迁移 + 语言切换副作用 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把老项目的 zh/en locale JSON 文件复制到新项目，修复请求 language 字段硬编码，并在语言切换时自动失效字典缓存、同步后端语言偏好。

**Architecture:** 三处改动：(1) cp locale JSON（老项目 en/zh → 新项目 en-US/zh-CN，仅支持这两种语言）；(2) `request.ts` 的 `signBody` 读取 `preferences.app.locale` 转短码；(3) 新 composable `useLocaleSync` 监听 locale 变化，触发 `invalidateDicts()` + `api.system.changeLanguage()`，在 `App.vue` 注册一次。

**Tech Stack:** Vue 3 Composition API、`@vben/preferences`、`@vben/stores`（useAccessStore）、TanStack Query（invalidateDicts）、`src/store/dict.ts`（已有 invalidateDicts）

---

## 文件清单

| 操作 | 文件 | 说明 |
| --- | --- | --- |
| 新建（cp） | `src/locales/langs/en-US/{17个文件}.json` | 从老项目 locales/en/ 复制 |
| 新建（cp） | `src/locales/langs/zh-CN/{17个文件}.json` | 从老项目 locales/zh/ 复制 |
| 修改 | `src/api/request.ts` | 替换硬编码 language，添加 resolveRequestLanguage |
| 新建 | `src/composables/use-locale-sync.ts` | watch locale 变化，触发两个副作用 |
| 修改 | `src/app.vue` | 调用 useLocaleSync() |

---

## Task 1：复制 locale JSON 文件

只支持 zh-CN 和 en-US，不引入 vi-VN/id-ID。

老项目路径：`/Users/cola/Documents/gs/feat1-ar_platform_admin/src/i18n/locales/` 新项目路径：`apps/web-ar-admin/src/locales/langs/`

**Files:**

- Create: `apps/web-ar-admin/src/locales/langs/en-US/{17个文件}`
- Create: `apps/web-ar-admin/src/locales/langs/zh-CN/{17个文件}`

- [ ] **Step 1: 复制 en-US locale 文件**

```bash
cp /Users/cola/Documents/gs/feat1-ar_platform_admin/src/i18n/locales/en/*.json \
   /Users/cola/Documents/gr/vue-vben-admin/apps/web-ar-admin/src/locales/langs/en-US/
```

- [ ] **Step 2: 复制 zh-CN locale 文件**

```bash
cp /Users/cola/Documents/gs/feat1-ar_platform_admin/src/i18n/locales/zh/*.json \
   /Users/cola/Documents/gr/vue-vben-admin/apps/web-ar-admin/src/locales/langs/zh-CN/
```

- [ ] **Step 3: 验证两套目录文件数**

```bash
for locale in en-US zh-CN; do
  count=$(ls /Users/cola/Documents/gr/vue-vben-admin/apps/web-ar-admin/src/locales/langs/$locale/ | wc -l | tr -d ' ')
  echo "$locale: $count 文件"
done
```

预期输出（原有 demos.json + page.json + 新增 17 个 = 19）：

```
en-US: 19 文件
zh-CN: 19 文件
```

- [ ] **Step 4: Commit**

```bash
git add apps/web-ar-admin/src/locales/langs/
git commit -m "feat(i18n): copy locale JSON files from old project (en-US/zh-CN only)"
```

---

## Task 2：修复 request.ts language 字段

`src/api/request.ts` 第 57 行 `const language = 'en'` 是硬编码占位符，需改为实时读取 `preferences.app.locale` 并转成后端短码。

**Files:**

- Modify: `apps/web-ar-admin/src/api/request.ts:1-10`（imports）和 `:56-67`（signBody）

- [ ] **Step 1: 在 request.ts 顶部添加 preferences import**

当前 `request.ts` 第 1-6 行：

```ts
import { RequestClient } from '@vben/request';
import { useAccessStore } from '@vben/stores';

import SparkMD5 from 'spark-md5';

import { message } from '#/adapter/naive';
```

改为（在 `@vben/stores` 下方新增一行）：

```ts
import { RequestClient } from '@vben/request';
import { preferences } from '@vben/preferences';
import { useAccessStore } from '@vben/stores';

import SparkMD5 from 'spark-md5';

import { message } from '#/adapter/naive';
```

- [ ] **Step 2: 在 signBody 上方添加 resolveRequestLanguage 函数**

在 `signBody` 函数定义（约第 56 行）的正上方插入：

```ts
/** BCP47 locale → 后端短码（preferences 已持久化，每次请求时读取） */
function resolveRequestLanguage(): string {
  return preferences.app.locale.split('-')[0]!;
}
```

- [ ] **Step 3: 替换 signBody 内的硬编码**

将：

```ts
function signBody(body: Record<string, unknown>): Record<string, unknown> {
  const language = 'en'; // Phase 1.5: replace with i18n locale
```

改为：

```ts
function signBody(body: Record<string, unknown>): Record<string, unknown> {
  const language = resolveRequestLanguage();
```

- [ ] **Step 4: TypeScript 编译检查**

```bash
pnpm --filter web-ar-admin exec tsc --noEmit 2>&1 | grep "request.ts" | head -20
```

预期：无 `request.ts` 相关错误。

- [ ] **Step 5: Commit**

```bash
git add apps/web-ar-admin/src/api/request.ts
git commit -m "fix(i18n): replace hardcoded language 'en' with resolveRequestLanguage()"
```

---

## Task 3：创建 useLocaleSync composable

监听 `preferences.app.locale` 变化，触发两个副作用：

1. `invalidateDicts()`：使所有字典 cache 失效，active useQuery 自动 background refetch
2. `api.system.changeLanguage()`：已登录时同步后端语言偏好（fire-and-forget）

跳过初始值（首次 watch 触发）和 locale 未变化的情况。

**Files:**

- Create: `apps/web-ar-admin/src/composables/use-locale-sync.ts`

注意：`src/composables/` 目录已存在（有 `dict/` 子目录）。

- [ ] **Step 1: 创建文件**

`apps/web-ar-admin/src/composables/use-locale-sync.ts`：

```ts
import { watch } from 'vue';

import { preferences } from '@vben/preferences';
import { useAccessStore } from '@vben/stores';

import { api } from '#/api';
import { invalidateDicts } from '#/store/dict';

/**
 * 监听语言切换，触发两个副作用：
 *   1. invalidateDicts() — 所有字典 cache 标记 stale，active useQuery 自动 refetch
 *   2. api.system.changeLanguage() — 已登录时同步后端语言偏好（fire-and-forget）
 *
 * 在 App.vue setup 中调用一次（全局单例，不需要 onUnmounted 清理）。
 */
export function useLocaleSync() {
  let skipInitial = true;

  watch(
    () => preferences.app.locale,
    async (locale, prevLocale) => {
      if (skipInitial) {
        skipInitial = false;
        return;
      }
      if (locale === prevLocale) return;

      invalidateDicts();

      const accessStore = useAccessStore();
      if (accessStore.accessToken) {
        api.system.changeLanguage().catch(() => {});
      }
    },
  );
}
```

- [ ] **Step 2: TypeScript 编译检查**

```bash
pnpm --filter web-ar-admin exec tsc --noEmit 2>&1 | grep "use-locale-sync" | head -20
```

预期：无 `use-locale-sync.ts` 相关错误。

- [ ] **Step 3: Commit**

```bash
git add apps/web-ar-admin/src/composables/use-locale-sync.ts
git commit -m "feat(i18n): add useLocaleSync — invalidate dicts + sync backend on locale change"
```

---

## Task 4：在 App.vue 注册 useLocaleSync

`App.vue` 是应用根组件，其 setup 在整个 app 生命周期内只运行一次，是注册全局单例 watcher 的正确位置。

**Files:**

- Modify: `apps/web-ar-admin/src/app.vue`（script setup 顶部）

- [ ] **Step 1: 在 App.vue script setup 中添加 import 和调用**

`src/app.vue` 当前 `<script lang="ts" setup>` 开头：

```ts
import type { GlobalComponentConfig, GlobalThemeOverrides } from 'naive-ui';

import { computed } from 'vue';

import { useNaiveDesignTokens } from '@vben/hooks';
import { preferences } from '@vben/preferences';
```

在 `import { preferences }` 下方添加：

```ts
import { useLocaleSync } from '#/composables/use-locale-sync';
```

然后在 `defineOptions({ name: 'App' });` 下方添加调用：

```ts
useLocaleSync();
```

- [ ] **Step 2: TypeScript 编译检查（全量）**

```bash
pnpm --filter web-ar-admin exec tsc --noEmit 2>&1 | head -40
```

预期：0 个新增错误（若有预存错误，确认与本次改动无关）。

- [ ] **Step 3: 开发服务器启动验证**

```bash
pnpm --filter web-ar-admin dev
```

打开浏览器，执行以下验证：

**验证 A：请求 language 字段**

1. 打开 DevTools → Network
2. 执行任意 API 请求（如刷新页面触发字典预热）
3. 检查请求 Payload → 确认 `language` 字段为 `"zh"`（默认中文）

**验证 B：切换语言后请求 language 变化**

1. 点击顶部导航栏语言图标 → 切换为 English
2. 触发任意 API 请求
3. 检查 Payload → 确认 `language` 字段变为 `"en"`

**验证 C：切换语言后字典 cache 失效**

1. 打开 DevTools → Network，筛选 `Dictionary` 相关请求
2. 切换语言
3. 确认有字典请求被重新发出（TanStack Query background refetch）

**验证 D：已登录时同步后端**

1. 登录后切换语言
2. Network 中确认有 `SysUsers/ChangeLanguage` 请求（POST，无 body 强制要求）

- [ ] **Step 4: Commit**

```bash
git add apps/web-ar-admin/src/app.vue
git commit -m "feat(i18n): register useLocaleSync in App.vue"
```

---

## 完成标志

所有 Task 完成后：

- [ ] `pnpm --filter web-ar-admin exec tsc --noEmit` 无新增错误
- [ ] 四套 locale 目录均有文件（en-US/zh-CN 各 19 个，vi-VN/id-ID 各 17 个）
- [ ] 切语言后 Network 请求中 `language` 字段跟随变化
- [ ] 切语言后字典 cache 重新请求（Background refetch 可在 DevTools 中观察到）
- [ ] 切语言后 `SysUsers/ChangeLanguage` 请求被发出（已登录状态）
